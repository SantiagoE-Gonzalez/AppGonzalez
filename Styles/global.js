import { StyleSheet } from "react-native";

export default globalStyle = StyleSheet.create ({
    container: {
        padding:10
    }
});

export const colors = {
    violeta: '#480ca8',
    celeste: '#4361ee',
    rosaOscuro: 'b5179e'
};